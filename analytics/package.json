{
  "name": "rikkahub-analytics",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute rikkahub-stats --file=schema.sql"
  }
}
